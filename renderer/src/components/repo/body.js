import React from 'react'
import styled from 'styled-components'

const BodyReposContainer = styled.div``

const BodyRepos = ({ current, list }) => {
    return <BodyReposContainer>body</BodyReposContainer>
}

export default BodyRepos
